const RightSidebar = ({ content }) => {
  return (
    <aside class="w-full md:w-[15%] bg-gray-200 p-4">
      <div class="w-full">{content}</div>
    </aside>
  );
};

export default RightSidebar;
